# LUMEN — Bugs
При исправлении бага - убрать из списка. Перепроверять исправление через скриншот - релизная версия.

## 29.09.2026 - 02:08 UTC+3

- [ ] Баг в мини-плеере. При наведении курсором на таймлайн (скролл), он улетает за экран. Проверить полосу времени, и исправить ошибку. Так же сделать окно без верхней полосы вверху, чисто одно видео и ничего лишнего, перетаскивать можно взявшись за любое место мини плеера. 

## 29.09.2026 - 00:12 UTC+3

- [x] В release сборке не работает эффект матового стекла (build.bat). В dev сборке (dev.bat) матовое стекло присутствует. Файлы одни и те же. ПКМ меню, таблетка видеоплеера, таблетка названия файла, другие UI элементы - просто полупрозрачный цвет. (Уточнение: эффект "матового стекла" работает в меню (дропдауне) "..." видеоплеера).
  - НАЙДЕНА И УСТРАНЕНА ПРИЧИНА (23.09, второй раунд): дело было НЕ в схеме `tauri.localhost`. Минификатор CSS (Lightning CSS в Tailwind 4) считал стандартный `backdrop-filter` и `-webkit-backdrop-filter` дубликатами и оставлял в собранном `.glass`/`.viewer-bar` ТОЛЬКО префиксный. Проверено вживую: Chromium вычисляет `-webkit-backdrop-filter` как `none` — префикс в WebView2 не работает. Попап "..." работал, потому что его blur задан инлайн-стилем React, который минификатор не трогает. Фикс: в `src/index.css` стандартное свойство объявлено ПОСЛЕ префиксного (минификатор хранит последнее объявление). Подтверждено сборкой: оба правила теперь содержат стандартный `backdrop-filter`. `useHttpsScheme` откатил — не причина. Проверить в релизной сборке скриншотом.

## 23.09.2026

- [ ] Подумать: остальные группы выбора (Плотность сетки, Скругления, Положение панели) привести к одному виду с «Разметкой главного экрана» (сейчас разметка - карточки с диаграммами, остальные - пилюли). Для двух вариантов карточки красивы, для трёх мелких - пилюли уместнее; возможно, оставить как есть, но решить осознанно.

## Исправлено (убрано из списка)

- 23.09 - версия в шапке (v0.1) не совпадала с «О программе» - маленькая таблетка возле названия приложения, значение из APP_VERSION.
- 23.09 - «Курсор-указатель на кнопках» указывал во всём окне - класс на `<html>` конфликтовал с утилитой Tailwind и курсор наследуется; заменено на data-атрибут, указатель только на контролах.
- 23.09 - идея «создать кастомный указатель» реализована: настройка «Использовать кастомный курсор» (по умолчанию выкл), нарисован фирменный курсор LUMEN (`assets/cursor.svg` + встроенные состояния в CSS).
- 23.09 - карточки «Разметка главного экрана» в мастере были почти одинаковы - диаграммы перерисованы: «Рельс» получил табличную форму (иконный рельс слева от полосы заголовка, тело ниже).
- 23.09 - «Разрешить выделение текста» скрыто; показывается после 5 нажатий на логотип в «О программе» (запоминается).
- 23.09 - «О программе» добавлена в левую навигацию настроек.
